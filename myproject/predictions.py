# precondition: cdata contains all fields correctly filled
def query_model(cdata): # dict of cleaned data from questionaire
    print("placeholder") # replace w calling logic
    print(f"answers are {cdata["answers1"]}, {cdata["answers2"]}, and {cdata["answers3"]}")
    return 1 # label, 1 for e and 0 for i